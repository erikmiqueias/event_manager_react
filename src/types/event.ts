export type Event = {
  id: string;
  event_name: string;
  event_details: string;
  event_date: string;
  duration: string;
  event_local: string;
  event_publicity: string;
  max_participants: number;
  price: number;
  duration_unit: string;
  event_format: string;
};
